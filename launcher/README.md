# Launcher (stub)

Phase 12 adds here: the PyApp launcher configuration, `install.sh` / `install.ps1` one-line installers,
and NSIS / DMG / AppImage packaging. Nothing is built from this folder yet.

All Python environments are created and updated through `uv`; the installers must never call `pip`.
