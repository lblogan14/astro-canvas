# Read-only rbcodes checkout used for the compatibility spike (not part of this repo)
/rbcodes/

# Design docs live only in the local checkout (owner's choice)
docs/designs/

# Python / uv
.venv*/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
coverage.json
htmlcov/
backend/dist/
packs/*/dist/

# Built SPA is generated by `task build` / CI, never committed
frontend/dist/
backend/src/astro_canvas/static/*
!backend/src/astro_canvas/static/.gitkeep

# Node (pnpm only; other lockfiles are refused)
node_modules/
package-lock.json
yarn.lock
bun.lockb
bun.lock

# Task runner
.task/

# Editors / OS
.idea/
.DS_Store
*.swp
backend/sdk/dist/

# Per-workspace engine state (created when a repo folder is used as a workspace)
.astro-canvas/

# Playwright MCP session artefacts
.playwright-mcp/
frontend/test-results/
frontend/playwright-report/

# claude
CLAUDE.md

# Windows PowerShell writes a module cache here when APPDATA is redirected (launcher tests).
/Microsoft/

# mkdocs build output.
/site/
