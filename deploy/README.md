# Deploy (stub)

Phase 12 adds here: `Dockerfile`, `compose.yaml`, and a `Caddyfile` for the lab-server tier
(uvicorn behind Caddy, data volume, optional OAuth proxy). The image installs the wheel with `uv`.
