# Registry seed

`index.json` is the seed for the git-backed node-pack registry (design §9). It is an empty list until
phase 11 publishes the first pack entries; the file will then move to its own repository.
